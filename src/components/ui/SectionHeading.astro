---
interface Props {
  title: string;
  subtitle?: string;
}

const { title, subtitle } = Astro.props;
---

<div class="mb-12">
  <h2 class="text-3xl font-bold text-text">{title}</h2>
  {subtitle && <p class="mt-2 text-text-muted text-lg">{subtitle}</p>}
</div>
