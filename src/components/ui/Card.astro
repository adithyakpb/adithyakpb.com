---
interface Props {
  class?: string;
  hover?: boolean;
}

const { class: className = '', hover = true } = Astro.props;
---

<div class:list={[
  'bg-card rounded-xl border border-border p-6',
  hover && 'card-hover',
  className,
]}>
  <slot />
</div>
